{% extends "layouts/page.njk" %}

{# Nested Components ------------------------------------------------------------------------------------------------------------------- #}

{% from "assets.njk" import blobContactLeft, blobContactRight %}

{# Values ------------------------------------------------------------------------------------------------------------------------------ #}

{% set data = successPage %}
{% set button = {
    props: {
        type: "button",
        size: "base",
        variant: "action",
        label: "Return home"
    },
    attrs: {
        href: "/home"
    }
} %}

{# ------------------------------------------------------------------------------------------------------------------------------------- #}

{% block blobs %}
    {{ blobContactLeft({class:"fixed left-0 bottom-0 w-[50vw] max-w-[646px]"}) }}
    {{ blobContactRight({class:"fixed right-0 bottom-0 w-[50vw] max-w-[712px]"}) }}
{% endblock %}

{# ------------------------------------------------------------------------------------------------------------------------------------- #}
