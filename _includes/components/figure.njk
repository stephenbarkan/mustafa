{# Nested Components ------------------------------------------------------------------------------------------------------------------- #}

{% from "snippets/loopAttrs.njk" import loopAttrs %}
{% from "components/button.njk" import button %}

{# Macro ------------------------------------------------------------------------------------------------------------------------------- #}

{% macro figure(props={}, attrs={}) %}

{# Values ------------------------------------------------------------------------------------------------------------------------------ #}

{# Theme styles ----------- #}

{% set figureStyles = "gap-6" %}
{% set figureImageStyles = "border border-neutral-lighter bg-neutral rounded-lg" %}
{% set figureCaptionStyles = "text-center text-body-base" %}

{# Other ------------------ #}

{# ------------------------------------------------------------------------------------------------------------------------------------- #}

<figure data-figure
    {{ loopAttrs(attrs) }}
    class="flex flex-col overflow-hidden {{ attrs.class }} {{ figureStyles }}">
    <div
        class="grow overflow-hidden
            {{ figureImageStyles }}
             {% if props.aspect %}
                {{ props.aspect }}
             {% else %}
                {% if props.iframe %}aspect-video{% endif %}
                {% if props.image %}aspect-square{% endif %}
            {% endif %}
        ">

        {% if props.image %}
            <img
                {% if props.lazy == false %}
                    fetchpriority="high"
                {% else %}
                    loading="lazy"
                {% endif %}
                {% set comma = joiner() %}
                eleventy:widths="
                    {% if props.widths.mobile %}
                        {{ props.widths.mobile }}px,
                    {% endif %}{% if props.widths.tablet %}
                        {{ props.widths.tablet }}px,
                    {% endif %}{% if props.widths.desktop %}
                        {{ props.widths.desktop }}px
                    {% endif %}"
                eleventy:formats="avif,webp,jpeg"
                sizes="
                    {%- if props.widths.mobile -%}
                        (max-width: 600px) {{ props.widths.mobile }},
                    {%- endif -%}{% if props.widths.tablet -%}
                        (max-width: 1040px) {{ props.widths.tablet }},
                    {%- endif -%}{% if props.widths.desktop -%}
                        {{ props.widths.desktop }}
                    {%- endif -%}"
                class="w-full h-full object-cover {{ props.focus }}" src="{{ props.image }}" alt="{{ props.alt }}">
        {% endif %}

        {% if props.iframe %}
            <iframe
                loading="lazy"
                class="h-full w-full object-cover aspect-video"
                src="{{ props.iframe }}"
                title="{{ props.caption }}"
                frameborder="0"
                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
                referrerpolicy="strict-origin-when-cross-origin"
                allowfullscreen>
            </iframe>
        {% endif %}

    </div>
    {% if props.caption %}<figcaption class="{{ figureCaptionStyles }}">{{ props.caption }}</figcaption>{% endif %}
</figure>

{% endmacro %}

{# Component Documentation ------------------------------------------------------------------------------------------------------------- #}

{% set figureMeta = {
    args: '{% call figure(
    props={
        image: "/assets/uploads/",
        iframe: "https://www.youtube.com/embed/",
        alt: "",
        caption: "",
        focus: "object-",
        aspect: "aspect-"
    },
    attrs={}
) %}
{% endcall %}',
description: "
-
-
-
"
} %}
